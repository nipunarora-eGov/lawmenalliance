import React,{lazy,Suspense} from 'react';
import ReactDOM from 'react-dom';


const App = () => {

  return (
    <div> LMA APP INIT</div>
  )
};

ReactDOM.render(<App />, document.getElementById('lma-root'));